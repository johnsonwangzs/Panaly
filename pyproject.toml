[tool.pytest.ini_options]
testpaths = ["tests"]
markers = ["local_data: regression checks using the existing resources directory"]

[tool.ruff]
target-version = "py310"
line-length = 100
extend-exclude = ["resources", "data", "outputs"]

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F", "I"]
